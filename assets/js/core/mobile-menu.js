/**
 * TOPCARE AI PLATFORM V2 — MOBILE MENU (PASSIVE COMPATIBILITY STUB)
 * Path: assets/js/core/mobile-menu.js
 * Version: 131.5.0 (BUILD 131.5 — PASSIVE COMPATIBILITY STUB)
 * Status: APPROVED & PERMANENTLY LOCKED
 * SRP: Maintains backward compatibility contract with bootstrap.js without side effects, 
 *      side-loading UI dependencies, or duplicate event listeners.
 */

export const MobileMenu = {
    /**
     * Passive compatibility init stub.
     * Prevents ImportError in bootstrap.js while delegating runtime event 
     * ownership cleanly to NavbarComponent.
     */
    init() {
        // No-op compatibility stub to isolate core layer from UI layer
    }
};

export default MobileMenu;