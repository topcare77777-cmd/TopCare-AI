/**
 * TOPCARE AI PLATFORM V2 — HEALTH MANIFESTS BARREL ARRAY
 * Path: assets/js/services/health/manifests/index.js
 * Status: ACTIVE (BUILD 126.1)
 */

import { HealthInquiryManifest } from './health.inquiry.manifest.js';

export const manifests = [
    HealthInquiryManifest
];

export default manifests;