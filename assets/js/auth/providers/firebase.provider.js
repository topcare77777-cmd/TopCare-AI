/**
 * TopCare AI Platform V2.0.0
 * Firebase Auth Provider Stub
 * Path: assets/js/auth/providers/firebase.provider.js
 */

class FirebaseAuthProvider {
    async login(email, password) { throw new ProviderError("Firebase not implemented"); }
    async register(name, email, password) { throw new ProviderError("Firebase not implemented"); }
    async forgotPassword(email) { throw new ProviderError("Firebase not implemented"); }
}